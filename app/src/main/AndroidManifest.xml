<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Autorise le trafic HTTP en clair vers le serveur local (LAN),
         Android bloque le HTTP non chiffré par défaut depuis l'API 28. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Pour "trouve-moi un restaurant près de moi" — recherche de lieux réels via OpenStreetMap. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Requis à partir d'Android 11 (API 30) : sans cette déclaration de visibilité de
         paquet, SpeechRecognizer peut échouer silencieusement à trouver le service de
         reconnaissance vocale du système sur certains appareils. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Momory"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
